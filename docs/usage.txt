usage: stown [-h] [-a {link,unlink}] [-b] [-d] [-l LEVEL] [-D DEPTH] [-f]
             target source [source ...]

Stow file system objects by managing symlinks

positional arguments:
  target                action target (links are created here)
  source                action sources (links point here)

options:
  -h, --help            show this help message and exit
  -a, --action {link,unlink}
                        action to take (default: link)
  -b, --absolute        create links using absolute paths
  -d, --dry-run         print operations only
  -l, --loglevel LEVEL  log level (default: DEBUG)
  -D, --depth DEPTH     maximum recursion depth (default: 10)
  -f, --force           force action (overwrites existing targets)

stown version 0.9.0-dev2 Copyright © 2025 Ralph Seichter
